<a href="/dashboard" class="btn waves-effect waves-light btn-flat"><i class="large material-icons">arrow_back</i></a>
<div class="container center-align">
<div class="row">
    <div class="col s12 m12 center-align">
        <h4>{{project.title}}
            <span>{{{editIcon project.user user project._id "projects" false }}}</span>
        </h4>
        <div class="card story">
            <div class="card-content">
                <span class="card-title">{{formatDate date 'MMMM Do YYYY, h:mm:ss a'}}</span>
                {{{project.body}}}
            </div>
        </div>
    </div> 
</div>

{{> _chart array=project.rating}}

</div>